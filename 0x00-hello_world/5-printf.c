#include <stdio.h>

/**
 * main - entry point
 * main_description: prints the phrase "with proper grammar, but the outcome...
 * Return: 0 if exited properly, non-zero otherwise
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
