#include <stdio.h>

/**
 * main - this is the entry point
 * return (0) - Gives 0 to indicate succesful running of program
 */
int main(void)
{
	puts("Programming is like building a multilingual puzzle");
	return (0);
}
